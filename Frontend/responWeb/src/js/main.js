$(() => {
	$('.owl-carousel').owlCarousel({
		items             : 1,
		loop              : true,
		autoplay          : true,
		autoplayTime      : 2000,
		lazyLoad          : true,
		autoplayHoverPause: true,
		nav : true
	});
});
